spring:
  datasource:
    url: jdbc:h2:mem:docai-test
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

  sql:
    init:
      mode: never

groq:
  api-key: test-api-key
  model: test-model